plugins {
    id 'java'
}

group 'de.scrupy'
version '1.0-SNAPSHOT'

subprojects {
    apply plugin: 'java'

    tasks.withType(JavaCompile).configureEach {
        options.encoding = "UTF-8"
    }

    repositories {
        mavenCentral()
    }
}